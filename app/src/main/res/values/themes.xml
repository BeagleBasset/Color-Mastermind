<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ColorMastermind" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:colorBackground">@color/gruv_bg</item>
        <item name="android:textColor">@color/gruv_fg</item>
    </style>

    <style name="Theme.ColorMastermind" parent="Base.Theme.ColorMastermind" />
</resources>